export const CONNECTIVITY_ECHO_PROTOCOL = '/connectivity-echo/1.0.0'
export const CONNECTIVITY_BULK_PROTOCOL = '/connectivity-bulk/1.0.0'

/** Max single-frame payload the server accepts (256 KiB). */
export const BULK_MAX_CHUNK_BYTES = 256 * 1024

export const DEFAULT_PUBSUB_PEER_DISCOVERY_TOPIC = '_peer-discovery._p2p._pubsub'
